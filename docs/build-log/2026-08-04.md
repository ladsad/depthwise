# 2026-08-04

- Implemented the core matching engine loop (sequential single-owner goroutine processing `OrderEvent`s).
- Implemented price-time priority matching with partial fills and cancel support.
- Added `Scenario1_BasicMatching` test and successfully verified it against the hand-verified fixture.
- Added `TestScenario1_ConcurrentCancelRace` to catch data races in concurrent order arrivals. Fixed the engine event loop to use a sequence-number reorder buffer, ensuring strictly deterministic execution even when concurrent orders arrive physically out-of-order over the channel (passed with `-race -count=20`).
